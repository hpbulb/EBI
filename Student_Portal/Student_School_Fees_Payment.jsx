export default function SchoolFees() {
    const navStyle = `bg-blue-900 px-4 py-4 rounded-2xl mx-4 my-5 shadow-lg flex jusitfy-between items-center`
    const textStyle = `text-md text-white font-sans font-semibold`

    return(
        <>
            <nav className={`${navStyle}`}>
                <div className={`${textStyle}`}>Welcome Admin!</div>
            </nav>
        </>
    )
}